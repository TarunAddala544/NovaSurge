apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: shopfusion
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
  - name: http
    port: 8003
    targetPort: 8003
    protocol: TCP
  type: ClusterIP
