# NovaSurge — Person 2 dependencies
# Install: pip install -r novasurge/requirements.txt

# Kubernetes Python client (injectors, remediators)
kubernetes>=29.0.0

# Async HTTP client (load generator, health polling, orchestrator)
httpx>=0.27.0

# Redis client (cache_flush remediator)
redis>=5.0.0

# asyncio is part of the Python standard library (3.4+)
# Listed here as documentation only — no install needed.
# asyncio
