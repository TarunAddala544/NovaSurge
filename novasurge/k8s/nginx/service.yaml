apiVersion: v1
kind: Service
metadata:
  name: nginx-gateway
  namespace: shopfusion
  labels:
    app: nginx-gateway
spec:
  selector:
    app: nginx-gateway
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
